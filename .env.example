# SQLite Database URL (Default local database path)
DATABASE_URL="file:./dev.db"

# Server Port (Railway automatically sets PORT dynamically)
PORT=3000

# Node Environment ('production' for Railway, 'development' for local testing)
NODE_ENV=production
